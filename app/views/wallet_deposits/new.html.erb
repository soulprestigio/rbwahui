<h1>New Deposit</h1>

<%= form_with(model: @transaction, url: wallet_deposits_path) do |form| %>
  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount, step: 0.01 %>
  </div>

  <div class="actions">
    <%= form.submit "Deposit" %>
  </div>
<% end %>